# UniFlow QA Agent — copy to .env and fill in. NEVER commit .env
LLM_PROVIDER=gemini

# Primary — https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Fallback — https://console.groq.com/keys
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
